How many spanning trees does the complete graph $K_n$ contain?
We create a sequence of length **$n - 2$**, called a Prüfer code, that corresponds to a labelled tree.
Encoding:
1. **Delete the leaf of lowest index and record the index of the adjacent vertex**.
2. **1::Repeat step 1 until an edge remains, creating a sequence of length $n-2$.**
Decoding:
1. **Let $b_1$ be the smallest value NOT in the code $a_{1},a_{2},\ldots a_{n-2}$. Create edge $a_1 \textasciitilde b_1$.**
2. **2::Delete $a_1$ in sequence and add $b_1$ to the end**
3. **2::Let $b_2$ be the smallest index NOT in $a_2,a_3\ldots a_{n-2},b_1$**
4. **2::Delete $a_2$ and add $b_2$ to end.**
5. **2::Repeat until the sequence is $b_1,b_2,\ldots b_{n-2}$. There will be $i\neq j,1\leq i,j\leq n$ NOT in the sequence. Create edge $i \textasciitilde j$ to create the tree.**

***

